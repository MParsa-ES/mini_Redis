#include <string>

struct Node
{
    Node *prev;
    Node *next;
    string value;
};

class LinkedList
{
private:
    /* data */
public:
    LinkedList(/* args */);
    ~LinkedList();
};

LinkedList::LinkedList(/* args */)
{
}

LinkedList::~LinkedList()
{
}
